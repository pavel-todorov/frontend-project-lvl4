import { createSlice } from '@reduxjs/toolkit';

export const channelsSlice = createSlice({
  name: 'channels',
  initialState: [],
  reducers: {
  }
});

// export const { increment, decrement, incrementByAmount } = channelsSlice.actions

export default channelsSlice.reducer
